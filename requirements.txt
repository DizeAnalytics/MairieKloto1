# Django Framework
# Version compatible avec Python 3.8+ (correction bug Python 3.14)
Django>=4.2.27

# Image processing library (recommandé pour Django, même avec FileField)
Pillow>=10.0.0

# Interface admin améliorée (version max dispo sur PyPI)
django-jazzmin>=3.0.1
django-admin-interface==0.32.0
django-colorfield>=0.9.0

# Database (SQLite est inclus avec Python, mais on peut ajouter d'autres backends si nécessaire)
# Pour PostgreSQL: psycopg2-binary>=2.9.0
# Pour MySQL: mysqlclient>=2.2.0

# Génération de PDFs
reportlab>=4.0.0

# Génération de fichiers Excel
openpyxl>=3.1.0

